server:
  port: 8080

spring:
  profiles:
    active: local
  application:
    name: najarang
  cloud:
    config:
      uri: http://localhost:8888
  data:
    web:
      pageable:
        default-page-size: 10
        one-indexed-parameters: true


